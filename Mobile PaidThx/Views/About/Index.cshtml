@{
    ViewBag.Title = "Index";
    ViewBag.PageID = "page-about";
    ViewBag.Classes = "";
}
<div data-role="page" id="@ViewBag.PageID" class="@ViewBag.Classes" data-add-back-btn="true" @TempData["DataUrl"]>
    @{ Html.RenderPartial("TitleBar"); }
    <div data-role="header" data-position="fixed" class="nav-pdthx" data-theme="c">
        <h1>About PaidThx</h1>
    @{Html.RenderPartial("WelcomeMenuTop", new Mobile_PaidThx.Models.MenuModel() { SelectedTabIndex = 3, HeaderText = "About" }); }
    </div>
    <div data-role="content">
        <h2>What is PaidThx?</h2>
        <p>PaidThx is a revolutionary way of exchanging money with friends, family and favorite organizations.</p>
        <h2>Watch this:</h2>
        <p>A short video </p>

    </div>
    @{Html.RenderPartial("WelcomeMenu", new Mobile_PaidThx.Models.MenuModel() { SelectedTabIndex = 3, HeaderText = "About" }); }
</div>

@section PageScripts{


}